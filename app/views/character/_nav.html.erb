<% content_for :secondary do %>
<nav id="secondary" class="user-nav">
  <div class="character-menu">
    <div class="pull-left">
      <div class="avatar">
        <%= character_portrait(@user.primary_character_id,'64') %>
      </div>
    </div>
    <div class="pull-left">
      <div class="title"><%= @character.name %></div>
    </div>
  </div>
  <ul class="secondary-nav-menu">
    <li><%= active_link_to 'Character Sheet', info_character_path(@user.primary_character_id), :class => 'icon-list-alt' %></li>
    <li><%= active_link_to 'Kill Log', killlog_character_path(@user.primary_character_id), :class => 'icon-screenshot' %></li>
  </ul>
</nav>
<% end %>
<% content_for :breadcrumb do %>
  <li class="active"><%= link_to @character.name, character_path(@user.primary_character_id) %></li>
<% end %>